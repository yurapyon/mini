todo

*: probably will do
-: might do
x: probably won't do

=== os/build ===
* make sure it compiles on linux
* set up installation
- typescript executor for web
    npm package?
* cleanup 'accept' flow when switching from reading from file->stdin

=== libraries ===
* random number generation
- posits
x 32bit math

=== vm ===
* better error handling
*   make sure abort works as intended
      compare to ansi forth
      catch stack over/underflow
*   stack traces
      could have a debug mode cli option
*   some type of exceptions ?
- search-order
- locals
