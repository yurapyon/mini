files:
  mini-out/
    precomiled.mini.bin        - main startup image, created as output of self-host.mini.fth
    precomiled-stable.mini.bin - backup startup image, for if there is an error in precompiled.mini.bin, created manually

  shell/
    build-fast.sh              - run 'zig build', optimizing for exe speed
    precompile-stable.sh       - compile the startup image, using precompiled-stable.mini.bin as the runtime
    precompile.sh              - compile the startup image, using precompiled.mini.bin as the runtime
    run.sh                     - run the forth repl, expects 'zig build' has already been called
    system.sh                  - run the virtual pc, expects 'zig build' has already been called
    wasm.sh                    - run the testing script in src/wasm

  web/                         - web demo

  src/
    examples/                  - more complete scripts
      cal.mini.fth             - prints a calendar
      citrus.mini.fth          - computer status bar
      home.mini.fth            - formats pwd for shell prompts
      life.mini.fth            - conways game of life
      vpc-drawing.mini.fth     - GUI program, for drawing
      vpc-life.mini.fth        - GUI program, conways game of life
      vpc-console.mini.fth     - [WIP] GUI program, simple terminal interface

    externals/                 - "libraries" of external functions
      os.zig                   - OS access
      floats.zig               - 32 bit floats
      dynamic.zig              - dynamic memory
      random.zig               - RNG
      root.zig                 - library root
      externals.zig            - struct for defining external functions

    sketch/                    - random unfinished/testing scripts

    system/                    - virtual PC
      content/                 - system resources (images)
      deps/                    - external libraries
      shaders/                 - GLSL shaders
      utils/                   - system utils

      c.zig                    - c bindings
      characters.zig           - character buffer
      gamepad.zig              - gamepad support
      image.zig                - images (the main data structure for working with pixel data)
      input-event.zig          - event channel
      math.zig                 - graphics math
      palette.zig              - structure to manage sets of colors
      pixels.zig               - pixel buffer
      resource-manager.zig     - for working with images and timers
      root.zig                 - library root
      system.mini.fth          - system startup file for mini
      system.zig               - main file for the virtual PC
      timer.zig                - abstraction for time and fps management

    utils/                     - zig utils

    lib/                       - main mini library
      bytecodes.zig            - bytecode behaviors and the code-field lookup table
      kernel.zig               - defines some types, contains execution loop
      memory.zig               - memory-related type definitions and error-returning memory access utils
      register.zig             - 'pointer' that stores an offset into forth memory
      stack.zig                - data/return stack struct
      root.zig                 - library root

    cli_options.zig            - basic commandline option interpreter
    main.zig                   - processes commandline options and starts the system
    self-host.mini.fth         - metacompiler to create a bytecode image for selfhosting
    startup.mini.fith          - a forth file that defines the 'std library' and most of the language features

